/*
let pizzaWeight = +(prompt('Введите вес пиццы в граммах:'));
let M = 10; // цена за 100грамм
let sale = 0.03;
let totalPrice = 0.01 * M * pizzaWeight;
(pizzaWeight >= 1000 || totalPrice >= 500) ? totalPrice *= (1 - sale) : totalPrice;
console.log(`Цена за пиццу: ${totalPrice}`);
*/